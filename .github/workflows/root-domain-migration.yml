name: Root domain migration

on:
  push:
    branches: [main]
    paths:
      - '.github/workflows/root-domain-migration.yml'

permissions:
  contents: write

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Migrate URLs to user root
        shell: bash
        run: |
          python - <<'PY'
          from pathlib import Path

          replacements = {
              'https://kayui-gavo.github.io/kewei-liu-site': 'https://kayui-gavo.github.io',
              'https://github.com/kayui-gavo/kewei-liu-site': 'https://github.com/kayui-gavo/kayui-gavo.github.io',
          }
          allowed = {'.html', '.xml', '.txt', '.md'}
          changed = []
          for path in Path('.').rglob('*'):
              if not path.is_file() or '.git' in path.parts or path.suffix.lower() not in allowed:
                  continue
              text = path.read_text(encoding='utf-8')
              new = text
              for old, repl in replacements.items():
                  new = new.replace(old, repl)
              if new != text:
                  path.write_text(new, encoding='utf-8')
                  changed.append(str(path))
          print('Updated:', ', '.join(changed) if changed else 'none')
          PY
      - name: Commit migration and remove temporary workflow
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          rm -f .github/workflows/root-domain-migration.yml
          rmdir .github/workflows 2>/dev/null || true
          rmdir .github 2>/dev/null || true
          git add -A
          if ! git diff --cached --quiet; then
            git commit -m "migrate site to GitHub Pages user root"
            git push
          fi
