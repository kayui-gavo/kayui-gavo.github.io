(()=>{const script=document.currentScript;const image=document.querySelector('.portrait');if(script&&image){const base=new URL('portrait-data/',script.src);const parts=['p01.txt','p02.txt','p03.txt','p04.txt','p05.txt','p06.txt'];Promise.all(parts.map(name=>fetch(new URL(name,base)).then(r=>{if(!r.ok)throw new Error('portrait');return r.text()}))).then(chunks=>{image.src=`data:image/webp;base64,${chunks.join('')}`}).catch(()=>{})}const page=document.querySelector('.page'),hero=document.querySelector('.hero'),portrait=document.querySelector('.portrait-wrap'),tabs=[...document.querySelectorAll('.focus-tab')],panels=[...document.querySelectorAll('.focus-copy')],reduced=matchMedia('(prefers-reduced-motion: reduce)').matches;document.documentElement.classList.add('motion-ready');const setMode=m=>{if(!hero)return;hero.dataset.mode=m;tabs.forEach(t=>t.setAttribute('aria-selected',t.dataset.mode===m?'true':'false'));panels.forEach(p=>p.classList.toggle('is-active',p.dataset.panel===m))};tabs.forEach(t=>{['mouseenter','focus','click'].forEach(ev=>t.addEventListener(ev,()=>setMode(t.dataset.mode)))});if(reduced||!page||!hero)return;let tx=0,ty=0,x=0,y=0,raf=0;const render=()=>{x+=(tx-x)*.08;y+=(ty-y)*.08;page.style.setProperty('--mx',x.toFixed(3));page.style.setProperty('--my',y.toFixed(3));if(portrait)portrait.style.transform=`perspective(1100px) rotateX(${(-y*1.15).toFixed(2)}deg) rotateY(${(x*1.45).toFixed(2)}deg) translate3d(${(-x*2.5).toFixed(1)}px,${(-y*1.8).toFixed(1)}px,0)`;if(Math.abs(tx-x)>.001||Math.abs(ty-y)>.001)raf=requestAnimationFrame(render);else raf=0};const kick=()=>{if(!raf)raf=requestAnimationFrame(render)};hero.addEventListener('pointermove',e=>{if(e.pointerType==='touch')return;const r=hero.getBoundingClientRect();tx=((e.clientX-r.left)/r.width-.5)*2;ty=((e.clientY-r.top)/r.height-.5)*2;kick()},{passive:true});hero.addEventListener('pointerleave',()=>{tx=ty=0;kick()})})();